# Choose the provider you want to use in the CLI.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna

# Required only when using --provider browser-use or --cloud-browser.
BROWSER_USE_API_KEY=
BROWSER_USE_MODEL=bu-2-0
